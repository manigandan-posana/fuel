# Admin Seed (You should replace this with real logic or database seed)
# For now, rely on First User = Admin

# Optional: Add initial project if needed via SQL or run logic.
# I will let the user create projects via API/UI.
