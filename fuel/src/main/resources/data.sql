-- Admin Seed (You should replace this with real logic or database seed)
-- For now, rely on First User = Admin logic in UserService

-- Optional: Add initial project if needed via SQL
-- For now, let the user create projects via API/UI
